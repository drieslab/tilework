% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tilePlan.R, R/pixelTilePlan.R,
%   R/spatialTilePlan.R, R/tileGroup.R
\name{bracket}
\alias{bracket}
\alias{[,tilePlan,numeric,missing,missing-method}
\alias{[,tilePlan,numeric,numeric,missing-method}
\alias{[,tilePlan,missing,missing,missing-method}
\alias{[,pixelTilePlan,numeric,numeric,missing-method}
\alias{[,spatialTilePlan,numeric,numeric,missing-method}
\alias{[,tileGroup,missing,missing,missing-method}
\alias{[,tileGroup,missing,.index,missing-method}
\alias{[,tileGroup,.index,missing,missing-method}
\alias{[,tileGroup,.index,numeric,missing-method}
\title{Extract Bounds from Iterator}
\usage{
\S4method{[}{tilePlan,numeric,missing,missing}(x, i, j, ..., drop = TRUE)

\S4method{[}{tilePlan,numeric,numeric,missing}(
  x,
  i,
  j,
  tile_fun = .spat_tile_bounds,
  fun = function(x) x,
  zero = FALSE,
  expand_grid = TRUE,
  drop
)

\S4method{[}{tilePlan,missing,missing,missing}(x, i, j)

\S4method{[}{pixelTilePlan,numeric,numeric,missing}(x, i, j, ..., drop = TRUE)

\S4method{[}{spatialTilePlan,numeric,numeric,missing}(x, i, j, ..., drop = TRUE)

\S4method{[}{tileGroup,missing,missing,missing}(x, i, j, ..., drop = TRUE)

\S4method{[}{tileGroup,missing,.index,missing}(x, i, j, ..., drop = TRUE)

\S4method{[}{tileGroup,.index,missing,missing}(x, i, j, ..., drop = TRUE)

\S4method{[}{tileGroup,.index,numeric,missing}(x, i, j, ..., drop = TRUE)
}
\arguments{
\item{x}{\code{tilePlan}}

\item{i}{numeric vector index if \code{j} is not given. Row index if \code{j} is also
present. Works like \code{matrix-like} indexing.}

\item{j}{numeric. Column index}

\item{...}{addtional params to pass (not used).}

\item{drop}{not used.}

\item{tile_fun}{(internal) function used to define tile bounds based on the
iterator object and ij indices.}

\item{fun}{(internal) post-processing function used to convert bounds to
expected output. Such as \code{ext()} for \code{spatialTilePlan}}

\item{zero}{logical. Whether to zero out buffer effects. (used by default with
\code{pixelTilePlan})}

\item{expand_grid}{logical (internal) whether to use \code{expand.grid()} on ij
indices.}
}
\value{
\code{list} of \code{numeric} or \code{SpatExtent}
}
\description{
Get a set of tile bounds from \code{tilePlan}. Values are always returned as
a \code{list}, even when length one to reduce surprises with \code{lapply()} usage.
}
\examples{
spat <- tilePlan("spatial")
ext(spat) <- c(0, 1000, 0, 1000)
length(spat) <- 9
spat[1]
spat[1,2:3]
spat[]

px <- tilePlan("pixel")
px$pxdims <- c(1000, 1000)
px$ncols <- 200
px$nrows <- 200
px[1]
px[2, 2:3]
px[]
}
