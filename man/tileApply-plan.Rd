% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tileApply.R
\name{tileApply-plan}
\alias{tileApply-plan}
\alias{tileApply,SpatRaster,missing,spatialTilePlan-method}
\alias{tileApply,SpatRaster,missing,pixelTilePlan-method}
\title{Basic Tile Processing}
\usage{
\S4method{tileApply}{SpatRaster,missing,spatialTilePlan}(
  x,
  FUN,
  tiles,
  lyr = NULL,
  future.seed = TRUE,
  log = FALSE,
  logpath = tempdir(),
  ...
)

\S4method{tileApply}{SpatRaster,missing,pixelTilePlan}(
  x,
  FUN,
  tiles,
  lyr = NULL,
  extend = TRUE,
  fill = NA,
  future.seed = TRUE,
  log = FALSE,
  logpath = tempdir(),
  ...
)
}
\arguments{
\item{x}{input data 1}

\item{FUN}{function to apply to each tile}

\item{tiles}{\code{tilePlan} inheriting object (\code{spatialTilePlan} or \code{pixelTilePlan})}

\item{lyr}{numeric. Layer number(s) to use (optional)}

\item{future.seed}{logical. Enable reproducible random seeds}

\item{log}{logical. Whether to log processing steps}

\item{logpath}{character. Log file path (if log = \code{TRUE})}

\item{...}{additional arguments passed to future.apply}

\item{extend}{whether to \link[terra:extend]{terra::extend} data to fit expected tile dimensions}

\item{fill}{numeric. Value to use use for new raster cells if \code{extend = TRUE}}

\item{y}{input data 2 (optional)}

\item{pad_y}{numeric. Additional padding applied to \code{y} tiling so \code{x} has full
spatial context of \code{y}}
}
\description{
Apply functions across \code{tilePlan}-inheriting objects.
}
\section{Special Function Parameters}{

Your \code{FUN} can optionally include these special parameters:
\itemize{
\item \code{.I} - tile number (integer)
\item \code{.TILE} - tile bounds/metadata
\item \code{.R} - tile row number
\item \code{.C} - tile column number
}
}

\examples{
f <- system.file("ex/elev.tif", package = "terra")
r <- terra::rast(f)

# Spatial tiling example
tp_spatial <- tilePlan("spatial")
ext(tp_spatial) <- ext(r)
length(tp_spatial) <- 4

# Apply function with tile metadata
results <- tileApply(r, tiles = tp_spatial, FUN = function(tile, .I, .R, .C) {
  list(
    tile_id = .I,
    position = paste0("row_", .R, "_col_", .C),
    mean_value = terra::global(tile, "mean", na.rm = TRUE)[[1]]
  )
})
force(results)

# Pixel tiling example
tp_pixel <- tilePlan("pixel")
tp_pixel$pxdims <- dim(r)[1:2]
tp_pixel$nrows <- 50
tp_pixel$ncols <- 50

# Save tiles to disk
outdir <- file.path(tempdir(), "tiles")
dir.create(outdir, showWarnings = FALSE)

tileApply(r, tiles = tp_pixel, FUN = function(tile, .I) {
  filename <- file.path(outdir, sprintf("tile_\%03d.tif", .I))
  terra::writeRaster(tile, filename, overwrite = TRUE)
  return(filename)
})

list.files(outdir)
unlink(outdir, recursive = TRUE)
}
\seealso{
\link{tileApply}, \code{\link[=tilePlan]{tilePlan()}}, \linkS4class{spatialTilePlan}, \linkS4class{pixelTilePlan}
}
