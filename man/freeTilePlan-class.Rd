% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes.R, R/freeTilePlan.R
\docType{class}
\name{freeTilePlan-class}
\alias{freeTilePlan-class}
\title{Free Tile Plan}
\description{
Tile plan defined by explicit per-tile bounds with no required uniformity
in size or spacing. The bounds matrix is the canonical representation —
there is no center-plus-dims formula. Always returns \code{SpatExtent} from
\verb{[i]}. Primary use case is as the output of adaptive spatial decomposition
algorithms such as quadtrees on vector/point data.

Tile plan defined by explicit per-tile bounds with no required uniformity
in size or spacing. Unlike grid-based plans, the bounds matrix is the
canonical representation — tile positions are not computed from a formula.

The primary use case is adaptive spatial decomposition of vector/point
data, where high-density regions require small tiles and sparse regions
can use large tiles. See \code{\link[=quadtreePlan]{quadtreePlan()}} for the iterative construction
workflow.
}
\section{Slots}{

\describe{
\item{\code{bounds}}{matrix. n x 4 matrix: xmin, xmax, ymin, ymax (one row per tile).}
}}

\note{
\verb{@tile_dims} is intentionally not populated. Any method that requires
uniform tile dimensions will not work with \code{freeTilePlan}.
}
\section{Setup}{

\itemize{
\item \code{freeTilePlan()} creates an empty instance.
\item \verb{$bounds<-} sets the n x 4 bounds matrix (\verb{xmin, xmax, ymin, ymax}).
Setting bounds triggers reinitialization of \verb{@n}, \verb{@dims}, and
\verb{@metadata}.
\item \verb{$pad} / \code{+} / \code{-} add spatial padding as with other plans.
}
}

\section{Notes}{

\itemize{
\item \verb{@tile_dims} is intentionally not populated. Operations that require
uniform tile dimensions (e.g. \code{extend = TRUE} in \code{\link[=getTile]{getTile()}}) are not
supported.
\item \code{nrow()} returns \code{n}, \code{ncol()} returns \code{1}, \code{length()} returns \code{n}.
\item \code{plot()} works via the existing spatial extent preview.
}
}

\examples{
tp <- freeTilePlan()
tp$bounds <- rbind(
    c(0,  50,  0,  50),
    c(50, 100, 0,  50),
    c(0,  50,  50, 100),
    c(50, 100, 50, 100)
)
length(tp)
tp[2]
plot(tp)
}
\seealso{
Other tile plans: 
\code{\link{freeTilePlan}},
\code{\link{pixelTilePlan-class}},
\code{\link{pointTilePlan-class}},
\code{\link{quadtreePlan}()},
\code{\link{spatialTilePlan-class}},
\code{\link{tilePlan}},
\code{\link{tilePlan-class}},
\code{\link{tilework-class}}

Other tile plans: 
\code{\link{freeTilePlan}},
\code{\link{pixelTilePlan-class}},
\code{\link{pointTilePlan-class}},
\code{\link{quadtreePlan}()},
\code{\link{spatialTilePlan-class}},
\code{\link{tilePlan}},
\code{\link{tilePlan-class}},
\code{\link{tilework-class}}
}
\concept{tile plans}
