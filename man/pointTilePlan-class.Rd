% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes.R, R/tilePlan.R, R/pointTilePlan.R
\docType{class}
\name{pointTilePlan-class}
\alias{pointTilePlan-class}
\alias{pointTilePlan}
\title{Point Tile Plan}
\usage{
pointTilePlan(input = c("spatial", "pixel"), output = input, ...)
}
\arguments{
\item{input}{character. Coordinate space of coords, dims, and padding:
\code{"spatial"} for CRS units, \code{"pixel"} for pixel indices.}

\item{output}{character. Bound type returned by \code{\link[=getTile]{getTile()}}. Defaults to
\code{input}. Cross-mode conversion requires a \code{SpatRaster} at \code{\link[=getTile]{getTile()}} time
(or \verb{@rast_dims}/\verb{@extent} set on the plan for standalone use).}
}
\description{
Tile plan where each tile is centered on a user-supplied (x, y) coordinate
with a uniform tile size. The center coordinates are primary — for use cases
like survey sampling, object detection patches, or site-centered extractions
where the point location is semantically meaningful.

Utility class for tiling centered on arbitrary (x, y) coordinates with a
uniform tile size. Unlike grid-based tile plans, tile placement is driven
entirely by the supplied point locations.

All coordinates, tile dims, and padding live in one consistent reference
frame set by \verb{$input} (\code{"spatial"} for CRS units, \code{"pixel"} for pixel
indices). The bound type returned by \code{\link[=getTile]{getTile()}} is a separate concern
controlled by \verb{$output} and resolved at extraction time.
}
\section{Slots}{

\describe{
\item{\code{coords}}{matrix. n x 2 matrix of tile center coordinates (columns: x, y).}

\item{\code{input}}{character. Coordinate space of coords, dims, and padding:
\code{"spatial"} for CRS units, \code{"pixel"} for pixel indices.}

\item{\code{output}}{character. Bound type returned by \code{\link[=getTile]{getTile()}}: \code{"spatial"} or
\code{"pixel"}. Defaults to \code{input}. Evaluated at \code{\link[=getTile]{getTile()}} time, not \verb{[i]}.}

\item{\code{rast_dims}}{numeric. \code{c(nrow, ncol)} of the reference raster. Required
for \code{plot()} when \code{input = "pixel"} and for cross-mode conversion without a
raster.}

\item{\code{extent}}{numeric. \code{c(xmin, xmax, ymin, ymax)} of the reference raster.
Required for pixel → CRS conversion without a raster.}

\item{\code{n}}{numeric. Number of tiles (equals number of input points).}

\item{\code{dims}}{integer. Always \code{c(n, 1L)}.}

\item{\code{tile_dims}}{numeric. Uniform \code{c(height, width)} in input coordinate units.}

\item{\code{pad}}{numeric. Tile padding in input coordinate units.}

\item{\code{metadata}}{data.frame. Per-tile metadata; always has \code{"tile"}, \code{"x"},
and \code{"y"} columns.}
}}

\section{Setup}{

\itemize{
\item \code{pointTilePlan()} creates an instance. \code{input} defaults to \code{"spatial"};
\code{output} defaults to \code{input}.
\item \verb{$coords<-} sets the n x 2 center coordinate matrix (columns: x, y).
\item \verb{$width<-} / \verb{$height<-} set the uniform tile width and height in input
coordinate units.
\item \verb{$input<-} / \verb{$output<-} change the coordinate / output mode.
\item \verb{$nrows<-} / \verb{$ncols<-} are aliases for \verb{$height<-} / \verb{$width<-} for
consistency with \linkS4class{pixelTilePlan} when using pixel coordinates.
\item \verb{$rast_dims<-} sets \code{c(nrow, ncol)} of the reference raster — required
for \code{plot()} when \code{input = "pixel"} and for cross-mode \code{getTile()} calls
when no raster is provided.
\item \verb{$extent<-} sets \code{c(xmin, xmax, ymin, ymax)} — required for
pixel → CRS conversion without a raster.
}
}

\section{Coordinate modes and required raster metadata}{
\tabular{lll}{
   Scenario \tab \verb{@rast_dims} \tab \verb{@extent} \cr
   \code{input = "spatial"}, \code{output = "spatial"} \tab not needed \tab not needed \cr
   \code{input = "pixel"}, \code{output = "pixel"} \tab for \code{plot()} \tab not needed \cr
   \code{input = "pixel"}, \code{output = "spatial"} \tab required \tab required \cr
   \code{input = "spatial"}, \code{output = "pixel"} \tab required \tab derived \cr
}


When \code{\link[=getTile]{getTile()}} is called with a \code{SpatRaster}, the raster's own metadata
is used for conversion — \verb{@rast_dims} and \verb{@extent} are only needed for
standalone operations (plotting, validation) without a raster.
}

\section{Getting tile bounds}{

\verb{[i]} returns bounds in the \strong{input} coordinate space: \code{SpatExtent} when
\code{input = "spatial"}, \code{integer[4]} when \code{input = "pixel"}. \verb{$output} is not
consulted at \verb{[i]} time. Use \code{\link[=getTile]{getTile()}} for output-mode conversion.
}

\section{Pixel mode and center ambiguity}{

When \code{input = "pixel"}, tile dims should be odd for an unambiguous center
pixel. With even dimensions \verb{center ± width/2} produces non-integer bounds
that are truncated by \code{as.integer()}.
}

\section{metadata}{

After setting coords, metadata is auto-populated with \code{"tile"}, \code{"x"}, and
\code{"y"} columns. Additional columns can be added via \verb{$<-}.
}

\examples{
tp <- pointTilePlan("spatial")
tp$coords <- cbind(x = c(10, 20, 30), y = c(5, 15, 25))
tp$width  <- 5
tp$height <- 5
tp

length(tp)
tp[1]
tp[2:3]
centroids(tp)
plot(tp)

# CRS input, pixel output (raster needed at getTile time)
tp2 <- pointTilePlan(input = "spatial", output = "pixel")
tp2$coords <- cbind(x = c(10, 20), y = c(5, 15))
tp2$width  <- 100
tp2$height <- 100
}
\seealso{
Other tile plans: 
\code{\link{freeTilePlan}},
\code{\link{freeTilePlan-class}},
\code{\link{pixelTilePlan-class}},
\code{\link{quadtreePlan}()},
\code{\link{spatialTilePlan-class}},
\code{\link{tilePlan}},
\code{\link{tilePlan-class}}

Other tile plans: 
\code{\link{freeTilePlan}},
\code{\link{freeTilePlan-class}},
\code{\link{pixelTilePlan-class}},
\code{\link{quadtreePlan}()},
\code{\link{spatialTilePlan-class}},
\code{\link{tilePlan}},
\code{\link{tilePlan-class}}
}
\concept{tile plans}
