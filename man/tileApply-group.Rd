% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tileApply.R
\name{tileApply-group}
\alias{tileApply-group}
\alias{tileApply,SpatRaster,missing,tileGroup-method}
\title{Hierarchical Tile Group Processing}
\usage{
\S4method{tileApply}{SpatRaster,missing,tileGroup}(
  x,
  FUN,
  tiles,
  parallel_strategy = c("groups", "tiles"),
  group_FUN = NULL,
  lyr = NULL,
  future.seed = TRUE,
  log = FALSE,
  logpath = tempdir(),
  simplify = FALSE,
  ...
)
}
\arguments{
\item{x}{input data 1}

\item{FUN}{function to apply to each tile}

\item{tiles}{\code{tileGroup} object}

\item{parallel_strategy}{character. \code{"groups"} to parallelize across groups,
\code{"tiles"} to parallelize within groups}

\item{group_FUN}{function. Optional function to apply to each group's results}

\item{lyr}{numeric. Layer number(s) to use (optional)}

\item{future.seed}{logical. Enable reproducible random seeds}

\item{log}{logical. Whether to log processing steps}

\item{logpath}{character. Log file path (if log = \code{TRUE})}

\item{simplify}{logical. Whether to flatten group results into single list}

\item{...}{additional arguments passed to future.apply}

\item{y}{input data 2 (optional)}

\item{pad_y}{numeric. Additional padding applied to \code{y} tiling so \code{x} has full
spatial context of \code{y}}

\item{group_sequential}{logical. If TRUE, process groups sequentially}
}
\description{
Apply functions across tileGroup objects with control over parallelization
strategy. Useful when tiles are organized into logical groups that need
different processing or aggregation.
}
\section{Parallelization Strategies}{

\itemize{
\item \code{"groups"} - Process groups in parallel, tiles within groups sequentially
\item \code{"tiles"} - Process groups sequentially, tiles within groups in parallel
}
}

\section{Special Function Parameters}{

Your \code{FUN} can optionally include these special parameters:
\itemize{
\item \code{.I} - tile number (integer)
\item \code{.TILE} - tile bounds/metadata
\item \code{.R} - tile row number
\item \code{.C} - tile column number
\item \code{.GROUP} - current group name (character)
}
}

\examples{
f <- system.file("ex/elev.tif", package = "terra")
r <- terra::rast(f)

# Create tile plan
tp <- tilePlan("spatial")
ext(tp) <- ext(r)
length(tp) <- 16

# Organize into groups (e.g., by geographic region)
tg <- tileGroup(tp, groups = list(
  "north" = 1:8,      # northern tiles
  "south" = 9:16,     # southern tiles
  "corners" = c(1, 4, 13, 16)  # corner tiles
))

# Process groups in parallel, with group-level aggregation
results <- tileApply(r, tiles = tg,
  parallel_strategy = "groups",
  FUN = function(tile, .I, .GROUP) {
    # Process individual tile
    list(
      tile_id = .I,
      group = .GROUP,
      stats = terra::global(tile, c("mean", "sd"), na.rm = TRUE)
    )
  },
  group_FUN = function(group_results, .GROUP) {
    # Aggregate results within each group
    means <- sapply(group_results, function(x) x$stats$mean)
    list(
      group = .GROUP,
      n_tiles = length(group_results),
      group_mean = mean(means),
      group_range = range(means)
    )
  }
)

# Results organized by group
str(results)
}
\seealso{
\link{tileApply}, \code{\link[=tileGroup]{tileGroup()}}, \linkS4class{tileGroup}
}
