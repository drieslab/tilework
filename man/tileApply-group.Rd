% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tileApply_tileGroup.R
\name{tileApply-group}
\alias{tileApply-group}
\alias{tileApply,token,missing,tileGroup-method}
\alias{tileApply,token,token,tileGroup-method}
\title{Hierarchical Tile Group Processing}
\usage{
\S4method{tileApply}{token,missing,tileGroup}(
  x,
  tiles,
  FUN,
  get_params_x = list(),
  parallel_strategy = c("groups", "tiles"),
  setup_FUN = NULL,
  group_FUN = NULL,
  callback_x = NULL,
  log = FALSE,
  logpath = getTileworkLogDir(),
  simplify = FALSE,
  parallel_params = list(),
  verbose = NULL,
  ...
)

\S4method{tileApply}{token,token,tileGroup}(
  x,
  y,
  tiles,
  FUN,
  get_params_x = list(),
  get_params_y = list(),
  pad_y = NULL,
  parallel_strategy = c("groups", "tiles"),
  setup_FUN = NULL,
  group_FUN = NULL,
  callback_x = NULL,
  callback_y = NULL,
  log = FALSE,
  logpath = getTileworkLogDir(),
  simplify = FALSE,
  parallel_params = list(),
  verbose = NULL,
  ...
)
}
\arguments{
\item{x}{input data 1}

\item{tiles}{\code{tileGroup} object}

\item{FUN}{function to apply to each tile}

\item{get_params_x}{named list. Additional params to pass to \code{\link[=getTile]{getTile()}} for
\code{x}}

\item{parallel_strategy}{character. \code{"groups"} to parallelize across groups,
or \code{"tiles"} to parallelize within groups}

\item{setup_FUN}{function. Optional per-group initialization function when
\code{parallel_strategy = "groups"}. Output is accessible within \code{FUN} as
\code{.SETUP_OUT}}

\item{group_FUN}{function. Optional function to apply to each group's results}

\item{callback_x}{function. Optional preprocessing function for \code{x} per
worker when \code{parallel_strategy = "groups"}}

\item{log}{logical. Whether to log processing steps}

\item{logpath}{character. Log file path (if log = \code{TRUE})}

\item{simplify}{logical. Whether to flatten group results into single list.
Group names will not be retained.}

\item{parallel_params}{named param list. See \link{parallel_params}}

\item{verbose}{verbosity. \code{TRUE}, \code{FALSE} or \code{"debug"} for more info on
stack tracing.}

\item{\dots}{additional params to pass to \code{\link[=bracket]{[}}}

\item{y}{input data 2 (optional)}

\item{get_params_y}{named list. Additional params to pass to \code{\link[=getTile]{getTile()}} for
\code{y}}

\item{pad_y}{numeric. Additional padding applied to \code{y} tiling so \code{x} has
full spatial context of \code{y}}

\item{callback_y}{function. Optional preprocessing function for \code{y} per
worker when \code{parallel_strategy = "groups"}}
}
\description{
Apply functions across tileGroup objects with control over parallelization
strategy. Useful when tiles are organized into logical groups that need
different processing or aggregation.

\strong{\code{token}} is a stand-in for any input data class (e.g. \code{SpatRaster},
\code{SpatExtent}, filepath, etc). See \link{redispatch_tileapply}
and \link{extending_tilework} for further information.
}
\section{Parallelization Strategies}{

\itemize{
\item \code{"groups"} - Process groups in parallel, tiles within groups sequentially
\item \code{"tiles"} - Process groups sequentially, tiles within groups in parallel
}
}

\section{Special Function Parameters}{

Your \code{FUN} can optionally include these special parameters:
\itemize{
\item \code{.I} - tile number (integer)
\item \code{.TILE} - tile bounds/metadata
\item \code{.R} - tile row number
\item \code{.C} - tile column number
\item \code{.GROUP} - current group name (character)
\item \code{.SETUP_OUT} - the output of \code{setup_FUN}
}

Your \code{setup_FUN} can optionally include these special parameters:
\itemize{
\item \code{.GROUP} - current group name (character)
\item \code{.X} - the input \code{x} object
\item \code{.Y} - the input \code{y} object (when provided)
}
}

\examples{
f <- system.file("ex/elev.tif", package = "terra")
r <- terra::rast(f)

# Create tile plan
tp <- tilePlan("spatial")
ext(tp) <- ext(r)
length(tp) <- 16

# Organize into groups (e.g., by geographic region)
tg <- tileGroup(tp, groups = list(
    "north" = 1:8, # northern tiles
    "south" = 9:16, # southern tiles
    "corners" = c(1, 4, 13, 16) # corner tiles
))

# Process groups in parallel, with group-level aggregation
results <- tileApply(r,
    tiles = tg,
    parallel_strategy = "groups",
    FUN = function(tile, .I, .GROUP) {
        # Process individual tile
        list(
            tile_id = .I,
            group = .GROUP,
            stats = terra::global(tile, c("mean", "sd"), na.rm = TRUE)
        )
    },
    group_FUN = function(group_results, .GROUP) {
        # Aggregate results within each group
        means <- sapply(group_results, function(x) x$stats$mean)
        list(
            group = .GROUP,
            n_tiles = length(group_results),
            group_mean = mean(means),
            group_range = range(means)
        )
    }
)

# Results organized by group
str(results)
}
\seealso{
\link{tileApply}, \code{\link[=tileGroup]{tileGroup()}}, \link[=tileGroup-class]{tileGroup}

Other tile processing:
\code{\link[=getBoundedData]{getBoundedData()}},
\code{\link[=getTile]{getTile()}},
\code{\link[=tileApply]{tileApply()}},
\code{\link{tileApply-iterator}},
\code{\link{tileApply-plan}}
}
\concept{tile processing}
