% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tileApply.R
\name{extending_tilework}
\alias{extending_tilework}
\title{Extending \{tilework\}}
\description{
\{tilework\} provides an extensible framework for:
\itemize{
\item \link[=tilePlan-class]{tile planning}
\item \link[=tileGroup-class]{tile selection}
\item \link[=tileIterator-class]{batch iteration}
\item \link[=tileApply]{parallelized data processing}
}
}
\section{Data flow}{


The framework accesses and processes data according to the following flow:
\enumerate{
\item \code{tileApply(x, tiles, ...)} # operate across tiles
\item \code{getTile(x, tiles, ..., get_params)} # get data for tiles
\enumerate{
\item \code{bounds <- tiles[...]} # generate bounds info
\item \code{getBoundedData(x, bounds, get_params)} # data extraction
}
}

Expanded roles:
\itemize{
\item \code{\link[=tileApply]{tileApply()}}-  handles the function parallelization strategy and logic.
\item \code{\link[=getTile]{getTile()}} - deals with any interactions between the data type and the tile
indexing. Operations that should be performed before extraction of the
actual data by tile bounds should happen at this level.
\item \code{\link[=getBoundedData]{getBoundedData()}} - performs the low-level data extraction from the data
source, provided with a set of bounds information (commonly a \code{SpatExtent} or
\code{numeric} of \code{c(xmin, xmax, ymin, ymax)})
}
}

\section{Adding new data type support}{


\{tilework\} functionalities can be extended to work with  other data
types/backends by creating a \code{\link[=getBoundedData]{getBoundedData()}} method for the data type.
Optional additional steps:
\itemize{
\item Method for \code{\link[=tileApply]{tileApply()}} should also be added if there are pre-run
validation/data collection steps needed.
\item Method for \code{\link[=getTile]{getTile()}} should be added if there are operations that affect
data/tile interactions.
}
}

\examples{
if (FALSE) {
    # Extending for a custom data type:
    setMethod(
        "getBoundedData", signature("MyDataType", "SpatExtent"),
        function(x, bound) {
            # Your data extraction logic here
            my_extract_function(x, bound)
        }
    )
}
}
\seealso{
Other tilework extension:
\code{\link{redispatch_tileapply,SpatRaster,freeTilePlan-method}},
\code{\link{token-class}}
}
\concept{tilework extension}
