% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intersect.R
\name{intersect}
\alias{intersect}
\alias{intersect,tilePlan,ANY-method}
\alias{intersect,freeTilePlan,ANY-method}
\alias{intersect,spatialTilePlan,ANY-method}
\title{Find tiles intersecting a spatial region}
\usage{
\S4method{intersect}{tilePlan,ANY}(x, y)

\S4method{intersect}{freeTilePlan,ANY}(x, y)

\S4method{intersect}{spatialTilePlan,ANY}(x, y)
}
\arguments{
\item{x}{\code{tilePlan}-inheriting object}

\item{y}{spatial query -- any object coercible via \code{ext()}, or a \code{SpatVector}
polygon}
}
\value{
\code{tileSelection}. Use \verb{$tile} to access tile indices.
}
\description{
Return a \code{tileSelection} of tiles whose padded bounds intersect the query
region \code{y}. Tile indices are available via \verb{$tile} on the result.

Accepts any object coercible via \code{ext()} for an axis-aligned query, or a
\code{SpatVector} polygon for an exact query (e.g. a back-projected parallelogram
from a rotated affine crop).

For \code{SpatVector} input, an AABB pre-cull is applied first, then exact
polygon-rectangle intersection is tested on candidates via \code{terra::relate()}.

\code{spatialTilePlan} uses an analytic grid formula for the AABB case (O(1)
range computation). \code{freeTilePlan} uses a vectorized bounds check. All
other \code{tilePlan} subclasses fall back to building polygons via \code{as.polygons()}.
}
\examples{
# spatialTilePlan -- axis-aligned query
tp <- spatialTilePlan(ext = c(0, 100, 0, 100), n = 16)
e <- ext(20, 60, 20, 60)
sel <- intersect(tp, e)
sel$tile          # indices of intersecting tiles
length(sel)       # number of tiles hit
plot(sel)
plot(e, add = T, border = "cyan")

# with padding -- tiles near the border are included
tp_pad <- tp + 5
sel_pad <- intersect(tp_pad, terra::ext(20, 60, 20, 60))
length(sel_pad) >= length(sel)  # TRUE: padding pulls in border tiles

# freeTilePlan -- quadtree bounds
fp <- freeTilePlan()
fp$bounds <- rbind(
    c(0,  50,  0,  50),
    c(50, 100, 0,  50),
    c(0,  50,  50, 100),
    c(50, 100, 50, 100)
)
intersect(fp, terra::ext(40, 60, 40, 60))$tile  # all 4 tiles touch the centre

# SpatVector polygon query (e.g. back-projected rotated crop)
corners <- rbind(c(30, 10), c(70, 10), c(90, 50), c(10, 50), c(30, 10))
poly <- terra::vect(corners, type = "polygons")
sel_poly <- intersect(tp, poly)
sel_poly$tile
plot(sel_poly)
plot(poly, add = T, border = "cyan")
}
\seealso{
Other tile* methods: 
\code{\link{arith}},
\code{\link{as.polygons}()},
\code{\link{bracket}},
\code{\link{centroids}()},
\code{\link{dim}()},
\code{\link{dollar}},
\code{\link{double_bracket}},
\code{\link{ext}()},
\code{\link{plot}()}
}
\concept{tile* methods}
