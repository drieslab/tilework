% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tileApply_tileIterator.R
\name{tileApply-iterator}
\alias{tileApply-iterator}
\alias{tileApply,token,missing,tileIterator-method}
\alias{tileApply,token,token,tileIterator-method}
\title{Streaming Tile Processing with Iterators}
\usage{
\S4method{tileApply}{token,missing,tileIterator}(
  x,
  tiles,
  FUN,
  get_params_x = list(),
  setup_FUN = NULL,
  callback_x = NULL,
  log = FALSE,
  logpath = getTileworkLogDir(),
  simplify = FALSE,
  parallel_params = list(),
  verbose = NULL,
  ...
)

\S4method{tileApply}{token,token,tileIterator}(
  x,
  y,
  tiles,
  FUN,
  get_params_x = list(),
  get_params_y = list(),
  pad_y = NULL,
  setup_FUN = NULL,
  callback_x = NULL,
  callback_y = NULL,
  log = FALSE,
  logpath = getTileworkLogDir(),
  simplify = FALSE,
  parallel_params = list(),
  verbose = NULL,
  ...
)
}
\arguments{
\item{x}{input data 1}

\item{tiles}{\code{tileIterator} object}

\item{FUN}{function to apply to each batch of tiles}

\item{get_params_x}{named list. Additional params to pass to \code{\link[=getTile]{getTile()}} for
\code{x}}

\item{setup_FUN}{function. Optional per-worker initialization function. Output
is accessible within \code{FUN} as \code{.SETUP_OUT}}

\item{callback_x}{function, (advanced). If provided, programmatic escape hatch for
preprocessing \code{x} per worker before \code{setup_FUN} and batch streamed processing
of \code{FUN} begins.}

\item{log}{logical. Whether to log processing steps}

\item{logpath}{character. Log file path (if log = \code{TRUE})}

\item{simplify}{logical. Whether to flatten results}

\item{parallel_params}{named param list. See \link{parallel_params}}

\item{verbose}{verbosity. \code{TRUE}, \code{FALSE} or \code{"debug"} for more info on
stack tracing.}

\item{\dots}{additional params to pass to \code{\link[=bracket]{[}}}

\item{y}{input data 2 (optional)}

\item{get_params_y}{named list. Additional params to pass to \code{\link[=getTile]{getTile()}} for
\code{y}}

\item{pad_y}{numeric. Additional padding applied to \code{y} tiling so \code{x} has full
spatial context of \code{y}}

\item{callback_y}{function, (advanced). If provided, programmatic escape hatch for
preprocessing \code{y} per worker before \code{setup_FUN} and batch streamed processing
of \code{FUN} begins.}
}
\description{
Apply functions using tileIterator objects for memory-constrained batch
processing. Ideal for very large datasets or when you need fine control
over processing workflow.

\strong{\code{token}} is a stand-in for any input data class (e.g. \code{SpatRaster},
\code{SpatExtent}, filepath, etc). See \link{redispatch_tileapply}
and \link{extending_tilework} for further information.
}
\section{Worker Distribution}{

\code{\link[=iterSplit]{iterSplit()}} is run with \verb{n =} \code{\link[future:nbrOfWorkers]{future::nbrOfWorkers()}} to distribute the
tiles to process across the expected number of workers, with each worker
processing its assigned tiles in batches.
}

\section{Special Function Parameters}{

Your \code{FUN} can optionally include these special parameters:
\itemize{
\item \code{.I} - tile number (integer)
\item \code{.TILE} - tile bounds/metadata
\item \code{.R} - tile row number
\item \code{.C} - tile column number
\item \code{.POSITION} - batch position range (start, end)
\item \code{.BATCH} - batch number within worker
\item \code{.SETUP_OUT} - the output of \code{setup_FUN}
}

Your \code{setup_FUN} can optionally include these special parameters:
\itemize{
\item \code{.W} - worker number
\item \code{.X} - the input \code{x} object.
\item \code{.Y} - the input \code{y} object (when provided)
}
}

\examples{
f <- system.file("ex/elev.tif", package = "terra")
r <- terra::rast(f)

# Create pixel tile plan
tp <- tilePlan("pixel")
tp$pxdims <- dim(r)[1:2]
tp$nrows <- 30
tp$ncols <- 30

# Create iterator for batch processing
iter <- tileIterator(tp, batch_size = 5)

# Process with worker initialization
results <- tileApply(r,
    tiles = iter,
    setup_FUN = function(.W, .X) {
        # Initialize per-worker state
        list(
            worker_id = .W,
            start_time = Sys.time(),
            raster_info = list(nrow = nrow(.X), ncol = ncol(.X))
        )
    },
    FUN = function(batch, .BATCH, .POSITION, .SETUP_OUT) {
        # Process batch of tiles
        batch_stats <- lapply(batch, function(tile) {
            terra::global(tile, "mean", na.rm = TRUE)[[1]]
        })

        list(
            worker = .SETUP_OUT$worker_id,
            batch_num = .BATCH,
            tiles_processed = .POSITION,
            batch_mean = mean(unlist(batch_stats))
        )
    }
)

# Check results structure
str(results)

# Example: Streaming processing for memory management
large_iter <- tileIterator(tp, batch_size = 3)
processed_count <- 0

while (large_iter$has_next) {
    batch <- getTile(r, large_iter)

    # Process batch
    batch_results <- lapply(batch, function(tile) {
        # Your processing here
        terra::global(tile, "mean")
    })

    processed_count <- processed_count + length(batch)
    cat("Processed", processed_count, "of", length(tp), "tiles\n")
}
}
\seealso{
\link{tileApply}, \code{\link[=tileIterator]{tileIterator()}}, \link[=tileIterator-class]{tileIterator}

Other tile processing:
\code{\link[=getBoundedData]{getBoundedData()}},
\code{\link[=getTile]{getTile()}},
\code{\link[=tileApply]{tileApply()}},
\code{\link{tileApply-group}},
\code{\link{tileApply-plan}}
}
\concept{tile processing}
