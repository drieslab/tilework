% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parallel.R
\name{parallel_params}
\alias{parallel_params}
\alias{getTileworkParMethod}
\alias{setTileworkParMethod}
\title{Parallel Processing Parameters}
\usage{
getTileworkParMethod()

setTileworkParMethod(method)
}
\arguments{
\item{method}{character. Either \code{"future"} (default) or \code{"biocparallel"}.
Which parallelization framework to use.}
}
\value{
\code{getTileworkParMethod} returns \code{character}. The parallelization
method being used.

\code{setTileworkParMethod} returns \code{NULL} invisibly
}
\description{
\code{\link[=tileApply]{tileApply()}} is parallelized through either \code{\link[future.apply:future_lapply]{future.apply::future_lapply()}}
(default) or \code{\link[BiocParallel:bplapply]{BiocParallel::bplapply()}}.

Additional parameters to control parallelization can be passed as a \code{list}
through the \code{parallel_params} argument in \code{tileApply()} methods.
}
\section{Parameters}{

Available parameters for the \code{parallel_params} list:
\itemize{
\item \code{method} - character. Either \code{"future"} (default) or \code{"biocparallel"}.
Which parallelization framework to use. Overrides \code{setTileworkParMethod()}
\item \code{future_seed} - logical (default = \code{TRUE}). Passed to the \code{future.seed}
parameter in \code{\link[future.apply:future_lapply]{future.apply::future_lapply()}}. Only used when
\code{method = "future"}.
\item \code{BPPARAM} - a \{BiocParallel\} parallelization parameter object. Only
used when \code{method = "biocparallel"}.
}

Additional parameters relevant to the underlying functions may also be
passed in the list and will be forwarded via \code{...}.
}

\section{Package Options}{

\itemize{
\item \code{"tilework.warn_sequential"} - logical (default = \code{TRUE}). Whether to
warn when using parallelization defaults that run sequentially
(i.e., \code{future::sequential()} or \code{BiocParallel::SerialParam()}).
\item \code{"tilework.par_method"} - character (default = \code{"future"}). Either
\code{"future"} or \code{"biocparallel"}. Which parallelization framework to use.
\item \code{"tilework.bpparam"} - a \{BiocParallel\} parameter object. Set this to
automatically use a specific BPPARAM, analogous to setting a
\code{\link[future:plan]{future::plan()}} for the \{future\} backend.
}
}

\examples{
getTileworkParMethod()

# Set to BiocParallel
setTileworkParMethod("biocparallel")
getTileworkParMethod()

\dontrun{
# Using future backend with multisession
future::plan(future::multisession, workers = 4)
tileApply(x, tiles, fun)

# Using BiocParallel backend
setTileworkParMethod("biocparallel")
options(tilework.bpparam = BiocParallel::SnowParam(workers = 4))
tileApply(x, tiles, fun)

# Or override per-call
setTileworkParMethod("future")  # global default is future
tileApply(x, tiles, fun, 
    parallel_params = list(method = "biocparallel")
) # but use BiocParallel here

# Suppress sequential warnings
options(tilework.warn_sequential = FALSE)
}
}
\seealso{
\code{\link[=tileApply]{tileApply()}}

Other parallel settings: 
\code{\link{tilework_management}}
}
\concept{parallel settings}
