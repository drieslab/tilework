% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/freeTilePlan.R
\name{quadtreePlan}
\alias{quadtreePlan}
\title{Adaptive Quadtree Tile Plan}
\usage{
quadtreePlan(
  x,
  tiles = NULL,
  FUN = nrow,
  threshold,
  min_tile_size = NULL,
  max_depth = 10L,
  verbose = NULL,
  ...
)
}
\arguments{
\item{x}{input data (e.g. \code{SpatVector} or file path).}

\item{tiles}{starting \code{tilePlan}. Defines the initial coarse grid.}

\item{FUN}{function. Applied to each tile; must return a single numeric.}

\item{threshold}{numeric. Tiles with \code{FUN(tile) > threshold} are subdivided.}

\item{min_tile_size}{numeric. Minimum tile side length (in CRS units).
Tiles below this size are kept as leaves regardless of density.}

\item{max_depth}{integer (default \code{10L}). Maximum subdivision depth.}

\item{verbose}{logical. Whether to be verbose.}

\item{...}{additional params passed to \code{tileApply()}.}
}
\value{
A \code{freeTilePlan} with an \code{n_records} metadata column containing
the last \code{FUN} value for each leaf tile.
}
\description{
Iteratively subdivides a spatial extent into a \code{freeTilePlan} by running
\code{FUN} on each tile and splitting any tile whose value exceeds \code{threshold}.
Uses \code{tileApply()} internally so each pass can be parallelized via
\code{\link[future:plan]{future::plan()}}.

\code{FUN} must return a single numeric scalar per tile — typically a point
count, density, or variance measure. Tiles are split into four equal
quadrants until all tiles are either below \code{threshold} or smaller than
\code{min_tile_size}.
}
\examples{
# dummy data
pts <- terra::vect(cbind(x = rnorm(1000, 0, 100), y = rnorm(1000, 0, 100)))
pts <- rbind(pts, terra::shift(pts, dx = 1000, dy = 1000))
plot(pts)
# data must exist on disk
f <- tempfile(fileext = "shp")
terra::writeVector(pts, f)
pts <- terra::vect(f, proxy = TRUE)

fp <- quadtreePlan(pts,
    threshold = 500L,
    min_tile_size = 1
)
plot(fp)
# plot with items per tile
plot(fp, values = "n_records")
}
\seealso{
Other tile plans:
\code{\link[=freeTilePlan]{freeTilePlan()}},
\code{\link{freeTilePlan-class}},
\code{\link{pixelTilePlan-class}},
\code{\link{pointTilePlan-class}},
\code{\link{spatialTilePlan-class}},
\code{\link[=tilePlan]{tilePlan()}},
\code{\link{tilePlan-class}},
\code{\link{tilework-class}}
}
\concept{tile plans}
