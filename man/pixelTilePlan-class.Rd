% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes.R, R/pixelTilePlan.R
\docType{class}
\name{pixelTilePlan-class}
\alias{pixelTilePlan-class}
\alias{pixelTilePlan}
\title{Pixel Tile Iterator}
\description{
Utility class for defining pixel-exact tiles of images in a format that is
easy to setup and manipulate using \code{$} and \verb{$<-} generics.
}
\section{Slots}{

\describe{
\item{\code{pxdims}}{pixel dimensions to iterate across}

\item{\code{n}}{numeric. Number of tiles to create.}

\item{\code{dims}}{numeric. Number of rows/cols in the array of tiles}

\item{\code{tile_dims}}{numeric. Row/col dimensions of each tile}

\item{\code{pad}}{numeric. Tile padding}

\item{\code{metadata}}{data.frame. Metadata per tile}
}}

\section{Getting tile pixel indices}{

\verb{[i]} and \verb{[i, j]} indexing can be used to select tiles, similarly to a
matrix. \verb{[]} without any indexing will return the entire set of indices
as a list. Indices are returned as lists of integer vectors of length 4,
with values xmin, xmax, ymin, ymax. Extracted vectors of indices will
have metadata attached via \code{attr()}. See metadata section below.
}

\section{padding}{

\code{+}/\code{-} can be used to add or subtract padding to each of the tiles.
Each bound will be expanded by the pad value. Pad values may only be
integer values.
}

\section{previewing tiles}{

\code{plot()} can be used to check the layout of the tiles.
}

\section{metadata}{

The \code{pixelTilePlan} object can contain metadata. By default after extent and
tiles setup, a column called \code{"tile"} will be set up that simply records
which tile it is.
\itemize{
\item \code{$} and \verb{$<-} can be used to get and set specific metadata, the \code{"pxdims"}
which are the pixel dimensions of the image to iterate across, \code{"ncols"}
and \code{"nrows"} which are the px dims of a tile, and the \code{"pad"} value.
\item \verb{[[i]]} selection will pull specific metadata rows corresponding to the
selected tiles.
}
}

\examples{
x <- tilePlan("pixel")
x$pxdims <- c(100, 100) # 100 px rows x 100 px cols to iterate across
x$ncols <- 20 # 20 px

length(x) # check how many tiles there are
dim(x)
nrow(x)
ncol(x)

# previewing
plot(x)

# tile padding
y <- x + 3
plot(y, alpha = 0.3) # red border shows the image space
# this is now larger than the original space.

# negative padding
z <- x - 5
plot(ext(c(0, 100, -100, 0)))
plot(z, add = TRUE)

# tile selection
x[5]
x[2, 2:3]

# metadata
x$tile
x$fname <- sprintf("tile_\%03d.tif", x$tile)
x[[5]]
x[[1:3]]$fname
}
